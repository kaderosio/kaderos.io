import LoginPageClient from "./login-client";

export const metadata = {
  title: "Login",
  description: "Bei KaderOS einloggen.",
};

export default function LoginPage() {
  return <LoginPageClient />;
}
