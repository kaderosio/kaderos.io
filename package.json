{
  "name": "kaderos",
  "version": "0.1.0",
  "private": true,
  "description": "KaderOS — Swiss AI Workforce Orchestration Platform",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "db:generate": "pnpm --filter @kaderos/db generate",
    "db:migrate": "pnpm --filter @kaderos/db migrate",
    "db:push": "pnpm --filter @kaderos/db push"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  }
}
